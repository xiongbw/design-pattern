# 单例模式

> **单例模式**是 `Java` 中最简单的设计模式之一。

## 作用
减少创建对象所消耗系统资源，保证在整个应用程序的生命周期中，任何时刻，类的实例都最多只存在一个。

## 实现

私有化构造方法，声明私有静态的对象，声明公有的静态的获得对象的方法。

### 懒汉式单例

> 声明对象时不创建对象，在获得对象的方法中创建对象。

优点：省内存。

缺点：在多线程中使用不安全，但可以用线程同步解决。

### 饿汉式单例

> 声明对象时创建对象，通过公有的静态方法获得对象。

优点：线程安全。

缺点：耗内存。